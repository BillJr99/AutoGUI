REQUIRED NEXT STEP: focus the new window before typing. Use desktop_activate_window (pass the window title or app name) or desktop_click at x=window.x+window.width//2, y=window.y+80 to land in the client area. Then call desktop_type. DO NOT stop here — the task is not complete until all steps (including any typing or further interaction) are done.
